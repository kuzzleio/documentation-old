kuzzle-guide:
  build: ./guide
  volumes:
    - ./source:/app/source
  ports:
    - 4567:4567

kuzzle-validation-reference:
  build: ./validation-reference
  volumes:
    - ./source:/app/source
  ports:
    - 4568:4567

kuzzle-dsl-reference:
  build: ./dsl-reference
  volumes:
    - ./source:/app/source
  ports:
    - 4569:4567

kuzzle-sdk-reference:
  build: ./sdk-reference
  volumes:
    - ./source:/app/source
  ports:
    - 4570:4567

#kuzzle-api-reference:
#  build: ./api-reference
#  volumes:
#    - ./source:/app/source
#  ports:
#    - 4571:4567